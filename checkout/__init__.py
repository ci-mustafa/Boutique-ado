# Specify the default application configuration class for the 'checkout' app.
# This ensures that the custom app configuration (CheckoutConfig) in apps.py
# is used, allowing for customized app behavior, signals, and settings.
default_app_config = 'checkout.apps.CheckoutConfig'
